pipeline{ 
    agent any
    tools {
        maven 'M2_HOME'
    }
    statges{
        
        stage('maven coommand'){
            steps{ 
                sh 'mvn clean compile install package'
            }
        }
        
    }
}